/// Equivalent to Moment class in Momento Patter
///
///Represents a single state of the editor class
///
class State {
  /// String content of the state
  final String content;

  /// constructor with [content] parameter
  State(this.content);
}
